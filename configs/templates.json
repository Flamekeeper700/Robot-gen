{
  "classTemplate": [
    "package ${package};",
    "",
    "${imports}",
    "",
    "public class ${className} {",
    "    // Generated Fields",
    "    ${fields}",
    "",
    "    public ${className}() {",
    "        // Initialization",
    "        ${initialization}",
    "    }",
    "",
    "    // Generated Methods",
    "    ${methods}",
    "}"
  ]
}